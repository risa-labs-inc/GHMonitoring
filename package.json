{
  "name": "ghmonitoring",
  "version": "1.0.0",
  "description": "GitHub task monitoring system",
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
